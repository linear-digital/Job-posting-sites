import React from 'react';
import HeroArea from './_UI/HeroArea';

const Home = () => {
    return (
       <section className="container mx-auto">
        <HeroArea />
    
       </section>
    );
};

export default Home;